const { marketMoversgetService } = require('../marketMovers.service.js');
const { makeApiCall } = require('../../utils/index.js');
jest.mock('../../utils/index.js');
describe('marketMoversgetService', () => {
  it('should pass the test', () => {
    //TODO
  });
});
